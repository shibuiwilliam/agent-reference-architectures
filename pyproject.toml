[project]
name = "agent-reference-architectures"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.5",
    "mkdocs-static-i18n>=1.2",
    "pymdown-extensions>=10.0",
    "pyyaml>=6.0",
]
