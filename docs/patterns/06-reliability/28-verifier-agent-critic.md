---
title: "Verifier Agent / Critic｜検証エージェント"
tags:
  - "信頼性・検証・ガードレール・自律"
  - "F2 失敗コスト"
  - "F4 レイテンシ予算"
---

# #28 Verifier Agent / Critic｜検証エージェント

!!! abstract "一言"
    生成エージェントとは**独立した検証器**が出力を検査し、基準を満たさなければ差し戻す。


<!-- BEGIN:GEN:meta -->
<details markdown="1">
<summary>メタデータ（機械可読） — #28 Verifier Agent / Critic｜検証エージェント</summary>

| 項目 | 値 |
|------|-----|
| **ID** | 28 |
| **カテゴリ** | 06-reliability — 信頼性・検証・ガードレール・自律 |
| **フォース** | `[F2]`, `[F4]` |
| **ダイヤル** | self-correction-loops |
| **二者択一** | inline-vs-post-verification, same-vs-different-model |
| **関連パターン** | #8, #27, #29 |
| **向き** | コード生成（テスト検証）、金融レポート、法務文書、公開コンテンツ |
| **不向き** | リアルタイムチャット; 失敗コスト低のブレインストーミング |
| **要素技術** | 別LLM/ルールチェッカ, ユニットテスト実行, URL検証, max-retry+timeout |

</details>
<!-- END:GEN:meta -->

## 概要

自分で書いたレポートの誤りに自分では気づきにくいのと同様に、生成と検証を同一のエージェントに任せると、自身の誤りを見逃しやすくなる（自己確証バイアス）。Verifier Agent は、生成されたドラフトを別のエージェント（Critic）が受け取り、事実整合性・フォーマット遵守・ポリシー違反などの観点で検査するパターンである。不合格であれば生成側へフィードバックを返して修正を要求する。このように生成と検証の関心を分離することで、検査基準を独立に進化させることができる。

!!! info "意思決定上の位置づけ"
    - **必要にするフォース**: `[F2]` 失敗コスト・`[F4]` レイテンシ予算
    - **関与する決定**: [相反](../../decisions/tradeoffs.md) の インライン↔事後検証・同一↔別モデル検証
    - **意思決定の進め方**: [意思決定の進め方](../../decisions/decision-flow.md)

## 設計

```mermaid
flowchart LR
    Gen[生成エージェント] -->|ドラフト| V[Verifier / Critic]
    V -->|合格| Out[最終出力]
    V -->|不合格 + 理由| Gen
```

検証の実装は複数の手法を組み合わせることができる。たとえば、LLMベースの批評（「この回答に事実誤認はないか」）、ルールベースのチェック（JSON Schema検証、禁止語検出）、外部ツール呼び出し（計算結果の再検算、URLの存在確認）などがある。リトライ回数には上限を設け、上限を超えた場合は人間へのエスカレーションか安全なフォールバックを返す。

## 解決する課題

失敗コストの高い `[F2]` 領域では、生成物をそのまま出荷することは許容されない。検証器を分離することで「何を・どの基準で検査したか」がトレースに残り、監査が可能になる。一方で、検証ステップには追加のLLM呼び出しが伴うため、レイテンシが増加する `[F4]`。そのため、検証の深さは失敗コストとレイテンシ予算のトレードオフで決まる。

## 向き / 不向き

- **向き**: コード生成（テスト実行で検証できる）、金融レポート、法的文書、顧客向け公開コンテンツに適している。
- **不向き**: 対話的なチャットでリアルタイム応答が求められる場面や、失敗コストが低いブレインストーミングの出力には向かない。

## 要素技術

- LLM Critic: 別プロンプト・別モデルでの批評生成
- ルール検証: JSON Schema、正規表現、禁止語リスト
- ツール検証: ユニットテスト実行、数式再計算、URL疎通確認
- ループ制御: 最大リトライ回数、タイムアウト

## 選定（相反）

- **検証の深さ ↔ レイテンシ** — 多段検証は品質を上げるがレイテンシが増大 `[F2][F4]`。失敗コストに比例して検証層を増やす。→ [相反の選定基準](../../decisions/tradeoffs.md)

## 関連パターン

- [#8 Planner-Executor-Reviewer](../02-composition/08-planner-executor-reviewer.md) — Reviewer ロールとして Verifier を組み込む構成
- [#27 Evidence-First Answer](27-evidence-first-answer.md) — 生成前の根拠確保と組み合わせて二重防御
- [#29 Guardrail Sidecar + Self-Correction](29-guardrail-sidecar-self-correction.md) — 生成パス上でインラインに検査するアプローチ
