# IMPROVEMENT.md

公開ドキュメント `agent-reference-architectures` の**全面再構成計画（根本再編版）**です。
今回の再構成は、これまでの「意思決定中心化＋機械可読化」をさらに一歩進め、**パターンリファレンス（59ページの独立カタログ）を解体し、その重要情報を「意思決定層」へ統合する**ことを中心とします。最終的に本ドキュメントは、**「本番システムにAIエージェントを組み込むソフトウェア設計の“意思決定”を支援するアーキテクチャパターン集」**になります。

---

## 0. このドキュメントは何か

これは公開ドキュメントを**意思決定中心へ根本再編するための計画**です。完成形ではなく「何を、なぜ、どの順で、どこまで」を定義します。これに従って Claude Code 等で改修を回せば、下記ゴールに必要な要素が体系的に揃います。

> ⚠️ **これまでの方針との最大の違い**：従来はパターン59ページを「語彙（リファレンス）」として残していました。**本計画ではパターンの独立リファレンスを廃止し、意思決定層に統合します。** パターンは「引ける深いカタログ」ではなく、「意思決定の結果として現れる具体構造（語彙）」として、意思決定ページの中・1枚の早見表・機械可読データに畳み込みます。

---

## 1. 方針と価値（最重要）

### 方針
> **意思決定層（程度の調整＝ダイヤル／相反する仕組みの選定基準＝二者択一）を中心に据えた、AIエージェントを本番システムへ組み込むためのソフトウェア設計“意思決定”のアーキテクチャパターン集。** パターンの網羅カタログではなく、**「どう決めるか」を背骨にしたドキュメント**にする。

### 価値
> **コーディングエージェントが開発時に本ドキュメントを読み込むことで、AIエージェントを含むソフトウェアアーキテクチャを適切に“意思決定”し、設計して人間に提案できること。** すなわち本ドキュメントは、アーキテクチャ設計の**意思決定サポート**である。

### 想定読者
- **人間**（アーキテクト・エンジニア）
- **コーディングエージェント**（Claude Code 等のLLM）

### 究極の到達点
要件と制約から **「フォース評価 → 二者択一の解決 → ダイヤル設定 → 具体構造（パターン）の確定 → 複合構成 → ADR記録」** を通しで実行し、引用付きで人間が承認できる設計提案を返せること。**最終判断は人間**（自カタログの検証・承認の思想をドキュメントの使われ方にも適用）。

---

## 2. 現状サーベイ（最新公開状態）

最新の公開サイトは、すでに意思決定中心の土台が整っています。

- **トップナビ**：「はじめに ／ 意思決定 ／ パターン・リファレンス（語彙） ／ 複合構成・アンチパターン ／ エージェント統合」。
- **意思決定層が厚い**：`decision-flow`（進め方）、特性 `C1–C9`（個別ページ）、フォース `F1–F9`（個別ページ）、ダイヤル20種（個別ページ）、二者択一16種（個別ページ）、`interactions`・`by-force`・`adr-template`・`worked-examples`・`parameterization`。
- **パターン**：12カテゴリ・**59ページの独立リファレンス**（今回の解体対象）。
- **複合構成・アンチパターン**：`reference-architectures`・`anti-patterns`・`pattern-index`。
- **エージェント統合**：`agent-guide`。
- 日英二言語。安定ID（`C#`/`F#`/`#N`/ダイヤル名/二者択一名）。`patterns.yml` が正本。

**つまり「意思決定層」はすでに主役の位置にあり、残るのは “パターン59ページという第2の主役” を解体して意思決定層へ一本化すること**です。

---

## 3. 最大の変更点 ── パターンリファレンスの解体と統合

### 3.1 何を消し、何を残すか

| 対象 | 扱い |
|---|---|
| `docs/patterns/**` の**59個の独立ページ**（深い概念解説） | **廃止**（人間向けの独立リファレンスとしては削除） |
| トップナビの「パターン・リファレンス（語彙）」タブ | **削除** |
| パターンの**識別子・名前（`#N`）** | **残す**（意思決定の結論を指す“語彙”として。引用・提案に必須） |
| パターンの**重要情報**（解決する課題・向き／不向き・調整・選定・要素技術・関連） | **意思決定層へ統合**（§3.2 の統合マップ） |
| パターンの**設計要点（構造）** | **1枚の「パターン早見表（語彙集）」**に1行〜数行で集約 ＋ `catalog.json` にフル保持 |
| `patterns.yml`（正本） | **残す**（語彙の正本。早見表・相互参照・catalog.json を生成） |

> **情報を捨てない**こと。「独立した深いページ」をやめるだけで、各ページが持っていた価値ある記述は、(a) 意思決定ページ本文、(b) 早見表の要約、(c) `catalog.json` のフィールド、のいずれかに**必ず移送**する。移送前に削除しない。

### 3.2 パターン情報の統合マップ（どのfacetをどの意思決定へ）

各パターンページの構成要素を、意思決定層の該当箇所へ移送する。

| パターンの構成要素 | 統合先（意思決定層） |
|---|---|
| 概要・設計（構造の要点） | **パターン早見表（語彙集）** の1行要約 ＋ 必要に応じ複合構成の構成要素として |
| 解決する課題（対応する `C#`/`F#`） | 該当 **フォース `F#` ページ**・**特性 `C#` ページ**・**フォース別逆引き `by-force`** の該当行 |
| 向き（適用すべき状況） | 関連する **二者択一の「選定基準」** ／ **ダイヤルの「目安」** に統合 |
| 不向き（避けるべき状況） | 同上（選定の反対側）／ **アンチパターン（意思決定の誤り）** に統合 |
| 調整（そのパターンが持つ目盛り） | 該当 **ダイヤルページ**の「関与する具体構造」欄 |
| 選定（属する相反） | 該当 **二者択一ページ**の選択肢（どちらを選ぶとどの構造になるか） |
| 要素技術 | **語彙集の備考** ／ 関連ダイヤル・二者択一の補足 |
| 関連パターン | **語彙集の関連** ／ `catalog.json` の `related[]` |

**統合の原則**：意思決定ページを読めば「この決定をどう下すか（フォース→目安／選択）」と「その結果どの具体構造（パターン）になるか」が**同じ場所で**分かる状態にする。パターンは“別の場所に引きに行くもの”ではなく、“決定の結論として目の前に出てくるもの”になる。

### 3.3 「パターン早見表（語彙集）」の新設（リファレンスの後継）

59ページの代わりに、**1枚の早見表ページ `docs/glossary.md`（語彙集）** を置く。`patterns.yml` から生成し、各行は：

`#N / 名前 / 一言要約（構造の核）/ 対応フォース[C#,F#] / 関与する決定（ダイヤル・二者択一へのリンク）/ 要素技術`

これは「深い解説」ではなく「**決定の結論を引くための索引**」。詳細が要るときは、リンク先の**意思決定ページ**（その構造が関与する決定）と `catalog.json` を参照する。

### 3.4 URL移行とリダイレクト（**必須・最重要**）

59ページの削除は、**多数の被リンク・公開URL・外部リンク**（例：解説ブログが全59パターンページへ張ったリンク）を破壊する。**情報移行とあわせて必ずリダイレクトを設定する。**

- `mkdocs-redirects` を導入し、旧 `/patterns/<category>/<NN-slug>/` を、**その構造が最も強く関与する意思決定ページ**または **早見表の該当アンカー** へ 301 相当でリダイレクト。
- リダイレクト表は `patterns.yml` から生成（各パターンに `primary_decision`（主たる統合先）フィールドを追加し、それを基に生成）。
- これにより、外部からの旧URLアクセスは「該当する意思決定」へ着地し、リンク資産を失わない。

---

## 4. 目標状態（再構成後の最終像）

### 4.1 トップナビ（パターンタブを廃止）
```
はじめに            ── 価値・対象読者（人間／エージェント）・読み方・選定の5問
意思決定（中核）     ── 進め方／特性C1–9／フォースF1–9／程度（ダイヤル20）／相反（二者択一16）／
                      相互作用／フォース別逆引き／パラメータ化／通し例／意思決定記録(ADR)
複合構成・アンチパターン ── 意思決定プリセット（旧リファレンスアーキ）／意思決定の誤り（旧アンチパターン）／パターン早見表（語彙集）
エージェント統合      ── 利用ガイド／提案テンプレ／取り込み経路／機械可読データ案内
```
**「パターン・リファレンス（語彙）」タブは削除。** パターンは「語彙集（早見表）」1枚＋各意思決定ページ内の言及＋`catalog.json` に存在する。

### 4.2 各意思決定ページの新しい中身（統合後）
- **ダイヤルページ（程度）**：小さすぎ／大きすぎ・決め手`[F#]`・目安（出発点）に加え、**「この目盛りを実装する具体構造（#N）」**欄を追加（旧パターンの調整情報を統合）。
- **二者択一ページ（相反）**：A／B・決定変数`[F#]`・デフォルト・ハイブリッドに加え、**「Aを選ぶとどの構造（#N）、Bを選ぶとどの構造（#N）」**＋各々の向き／不向き（旧パターンの選定・向き不向きを統合）。
- **フォースページ `F#`／逆引き `by-force`**：そのフォースが高い/低いときに効く**決定とパターン（#N）**（旧パターンの「解決する課題」を統合）。
- **特性ページ `C#`**：その特性が引き起こす設計圧力と、対応する決定・構造（#N）。

### 4.3 複合構成 ＝「意思決定プリセット」
旧リファレンスアーキテクチャを、**フォースプロファイル → 決まった構成（採用ダイヤル／二者択一／パターン#N）** の**プリセット**として位置づけ直す。「選定の5問」を入口に。各構成は早見表・各決定ページへリンク。

### 4.4 アンチパターン ＝「意思決定の誤り」
過大タイムアウト・過剰ガードレール・最強モデル一択・何でも同期/非同期・万能マルチエージェント等を、**程度の誤り／相反の誤選択**として分類し、該当ダイヤル・二者択一へリンク（旧パターンの「不向き」を吸収）。

### 4.5 機械可読データ（パターンは“語彙”として残す）
- **`/catalog.json`**：`forces` / `characteristics` / `dials` / `tradeoffs` / `reference_architectures(presets)` / `rules` / **`patterns`（語彙：id・name・summary・design・forces・dials・tradeoffs・related・element_tech・primary_decision・when/when_not）**。深い設計記述はここに保持し、人間ページから消えても機械は失わない。
- **`/llms.txt`・`/llms-core.txt`・`/llms-full.txt`**：意思決定中心の索引／コア／全文。
- すべて `patterns.yml`＋`decisions.yml` から生成。

### 4.6 エージェント提案テンプレ（出力様式・据え置き）
```
## アーキテクチャ提案 v{catalog_version}
1. 要件と制約の要約
2. フォース評価（C1–9 / F1–9：高/中/低 と根拠）
3. 解いた二者択一（選択・根拠フォース・不採用案）
4. 設定したダイヤル（初期値＝目安・調整方針）
5. 確定した具体構造（パターン #N と採用理由＝効いたフォース）
6. 複合構成（プリセットとの対応）
7. リスクと対応（意思決定の誤り＝アンチパターン）
8. 代替案とトレードオフ
9. 未解決の論点（人間に確認したいこと）
> 本提案はカタログ v{x} に基づきます。最終判断は人間が行ってください。
```

---

## 5. 単一正本と生成パイプライン

```
patterns.yml（語彙の正本：#N・名前・要約・設計・forces・dials・tradeoffs・related・element_tech・primary_decision）
decisions.yml（C/F/ダイヤル/二者択一/プリセット/rules/version）
        │
        └─► scripts/generate.py ──► docs/glossary.md（パターン早見表＝語彙集）
                                  ├─► 各ダイヤル/二者択一/F#/by-force ページへ「関与する構造(#N)」をマーカー間生成
                                  ├─► docs/catalog.json（patterns をフル保持）
                                  ├─► docs/llms.txt / llms-core.txt / llms-full.txt
                                  ├─► リダイレクト表（旧 /patterns/** → 統合先）
                                  └─► docs/pattern-index.md（早見表に統合 or 置換）
```
- **59ページの生成は廃止**。`patterns.yml` は「語彙＋統合先」を定義する正本として残し、ページではなく**早見表・相互参照・機械データ**を生成する。
- 生成は冪等（`<!-- BEGIN:GEN:xxx -->`〜`<!-- END:GEN:xxx -->` のマーカー間のみ置換）。

---

## 6. 実装プラン（フェーズ）

依存に沿って段階実装。各フェーズ末に `mkdocs build --strict` と `generate.py` 冪等を確認。

### Phase 0 ── 計画・退避（破壊の前に）
- 作業ブランチ `feat/decision-only-restructure`。
- **現状の全URL・全パターン本文を退避**：`patterns.yml` に各パターンの `summary`/`design`/`when`/`when_not`/`element_tech`/`related`/`primary_decision` を**完全移送**（既存59ページから抽出して埋める）。これが「情報を捨てない」担保。
- 旧→新のリダイレクト対応表（`primary_decision` ベース）を設計。

### Phase 1 ── 統合先の器を用意
- `decisions.yml` を整備（既存の意思決定ページ内容を正本化）。
- 各ダイヤル／二者択一／`F#`／`by-force` ページに `GEN:patterns` マーカーを設置し、`patterns.yml` から「関与する構造（#N）＋向き／不向き」を生成注入。
- `docs/glossary.md`（パターン早見表＝語彙集）を生成。
- `catalog.json` に `patterns`（フル情報）を出力。

### Phase 2 ── パターンリファレンスの撤去とリダイレクト
- `mkdocs.yml` の nav から「パターン・リファレンス（語彙）」を削除。
- `docs/patterns/**` を削除（Phase 0–1 で移送済みであることを確認後）。
- `mkdocs-redirects` で旧 `/patterns/**` → 統合先（意思決定ページ or 早見表アンカー）を設定（生成表を使用）。
- `reference-architectures` を「意思決定プリセット」、`anti-patterns` を「意思決定の誤り」に再フレーム。`pattern-index` は `glossary` に統合 or リダイレクト。

### Phase 3 ── エージェント消費の完成と検証
- `/agent-guide/` を意思決定中心の使い方へ更新（手順・引用規約・境界規約・提案テンプレ・最終判断は人間）。`AGENTS.md` 同期。
- `llms.txt`/`llms-core.txt`/`llms-full.txt` 再生成、`CHANGELOG.md`・`version` 更新（破壊的変更＝メジャー更新）。
- JP/EN 整合、リンク監査、最終検収（§8）。

---

## 7. 不変条件・制約

- **情報を捨てない**：59ページ削除の前に、価値ある記述を `patterns.yml`／`catalog.json`／早見表／各決定ページへ**完全移送**してから削除する。
- **リンクを壊さない**：旧 `/patterns/**` は**必ずリダイレクト**を張る（外部・内部の被リンク保護）。
- **パターン番号・slug・`#N` 表記を変えない**（語彙の同一性・引用の安定性）。`patterns.yml` を正本に維持。
- **単一正本・冪等生成**：人間散文と生成部はマーカーで分離。CIで毎回再生成。
- **JP/EN 同期**：機械可読のIDは言語非依存。
- **エージェント安全規約**：カタログ内のみ／不確実は明示／**最終判断は人間**。
- **`site/` を編集・コミットしない**。
- **破壊的変更の明示**：URL構造が変わるため `version` をメジャー更新し `CHANGELOG.md` に記載。

---

## 8. 品質ゲート・最終検収（すべて満たすまで完了としない）

- [ ] `mkdocs build --strict` がエラーゼロ（生成物は `not_in_nav`、リンク切れなし）
- [ ] nav に「パターン・リファレンス」タブが**存在しない**
- [ ] `docs/patterns/**` が**削除**され、旧URLが**すべてリダイレクト**で着地する（リダイレクト表が全59をカバー）
- [ ] 各ダイヤル／二者択一／`F#`／`by-force` ページに、関与する具体構造（#N）と向き／不向きが**統合表示**されている
- [ ] `docs/glossary.md`（パターン早見表）が59件すべてを1行索引で網羅し、各々が意思決定ページへリンク
- [ ] `/catalog.json` が `patterns`（summary/design/forces/dials/tradeoffs/related/element_tech/primary_decision）を**フル保持**し、59件＋C/F/dials/tradeoffs/presets/rules/version を含む
- [ ] `patterns.yml`／`decisions.yml` 編集 →`python scripts/generate.py` 再実行で全成果物が**冪等再生成**
- [ ] 旧59ページの価値ある記述が、決定ページ・早見表・catalog.json の**いずれかに移送済み**（情報損失ゼロの確認）
- [ ] `/agent-guide/`＋`AGENTS.md`：手順・引用規約・境界規約・提案テンプレ・**最終判断は人間**が完備
- [ ] `/llms.txt`・`/llms-core.txt`・`/llms-full.txt` 公開、`CHANGELOG.md`＋`version`（メジャー更新）
- [ ] JP/EN の主要ページ整合、`grep -rn "TODO" docs/` 空、プレースホルダ残ゼロ
- [ ] `git status` クリーン（`site/` 未コミット）

---

## 9. 留意点（やり過ぎ・失敗を避ける）

- **「削除」より先に「移送」**。情報を失ってからでは取り返せない。Phase 0–1 の移送完了を Phase 2 の削除の前提にする。
- **語彙は残す**。コーディングエージェントは提案で具体構造を名指す必要がある。`#N` の名前と1行要約・機械データは**必ず維持**する（“引ける深さ”は減らすが“名指せること”は守る）。
- **リダイレクトは交渉不可**。外部ブログ等が全59ページへリンクしている。リダイレクト無しの削除はリンク資産の破壊。
- **意思決定ページを肥大させすぎない**。各ページは「決め方（フォース→目安／選択）」が主、統合した構造情報（#N）は**簡潔に**。深い詳細は早見表＋catalog.json＋プリセットへ。
- **人間レビュー前提**。エージェントの提案は確定でなく案。最終判断は人間。
- **過剰な形式化を避ける**。決定規則は「IF–THEN 候補提示」まで。断定は人間に委ねる。

---

## 10. 次の一手（Claude Code への引き渡し）

本計画は **Phase 0（移送）→ 1（統合）→ 2（撤去＋リダイレクト）→ 3（仕上げ）** の順で着手する。Claude Code には本ファイル `IMPROVEMENT.md` を正本として渡し、論理単位ごとにコミットし、各フェーズ末に `mkdocs build --strict` と `generate.py` 冪等を確認させながら自律実行させる（`PROMPT-EXECUTE-IMPROVEMENT.md` 形式の実行プロンプトへ展開可能）。

> 本再構成の核心はひとつ——**パターンを「引く対象」から「決定の結論」へ降ろし、ドキュメントの背骨を完全に意思決定へ一本化すること。** これにより、人間もコーディングエージェントも「どう決めるか」を辿るだけで、根拠（フォース）→ 目盛り（ダイヤル）→ 選択（二者択一）→ 具体構造（パターン#N）→ 複合構成 → 記録（ADR）まで、ひと続きに設計・提案できるようになる。